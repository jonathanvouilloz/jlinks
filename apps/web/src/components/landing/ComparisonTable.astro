---
// Comparison table data
const features = [
  { key: 'links', linktreeFree: true, linktreePro: true, jlinks: true },
  { key: 'customColors', linktreeFree: 'partial', linktreePro: true, jlinks: true },
  { key: 'proThemes', linktreeFree: false, linktreePro: true, jlinks: '10' },
  { key: 'brandedQr', linktreeFree: false, linktreePro: true, jlinks: true },
  { key: 'addContact', linktreeFree: false, linktreePro: false, jlinks: true },
  { key: 'customDomain', linktreeFree: false, linktreePro: true, jlinks: 'pro' },
  { key: 'analytics', linktreeFree: 'basic', linktreePro: true, jlinks: 'pro' },
  { key: 'noBranding', linktreeFree: false, linktreePro: true, jlinks: 'pro' },
];
---

<section class="landing-section landing-section--alt comparison">
  <div class="landing-container">
    <h2 class="comparison__title" data-animate data-i18n="comparison.title">
      Ce que tu paies ailleurs est gratuit ici.
    </h2>

    <div class="comparison-table" data-animate>
      <div class="comparison-table__grid">
        <!-- Header row -->
        <div class="comparison-table__cell comparison-table__cell--header"></div>
        <div class="comparison-table__cell comparison-table__cell--header" data-i18n="comparison.linktreeFree">
          Linktree Free
        </div>
        <div class="comparison-table__cell comparison-table__cell--header comparison-table__cell--header-inline">
          <span data-i18n="comparison.linktreePro">Linktree Pro</span>
          <span class="comparison-table__price-tag">$9</span>
        </div>
        <div class="comparison-table__cell comparison-table__cell--header comparison-table__cell--highlight" data-i18n="comparison.jlinks">
          jLinks
        </div>

        <!-- Feature rows -->
        {features.map((feature) => (
          <>
            <div class="comparison-table__cell comparison-table__cell--feature" data-i18n={`comparison.features.${feature.key}`}>
              {feature.key}
            </div>
            <div class="comparison-table__cell">
              {feature.linktreeFree === true && <span class="comparison-table__check">&#10003;</span>}
              {feature.linktreeFree === false && <span class="comparison-table__cross">&#10005;</span>}
              {feature.linktreeFree === 'partial' && <span class="comparison-table__partial">~</span>}
              {feature.linktreeFree === 'basic' && <span class="comparison-table__text" data-i18n="comparison.basic">Basique</span>}
            </div>
            <div class="comparison-table__cell">
              {feature.linktreePro === true && <span class="comparison-table__check">&#10003;</span>}
              {feature.linktreePro === false && <span class="comparison-table__cross">&#10005;</span>}
            </div>
            <div class="comparison-table__cell comparison-table__cell--highlight">
              {feature.jlinks === true && <span class="comparison-table__check">&#10003;</span>}
              {feature.jlinks === false && <span class="comparison-table__cross">&#10005;</span>}
              {feature.jlinks === '10' && <span class="comparison-table__check">&#10003; (10)</span>}
              {feature.jlinks === 'pro' && <span class="comparison-table__text" data-i18n="comparison.pro">Pro</span>}
            </div>
          </>
        ))}

        <!-- Price row -->
        <div class="comparison-table__cell comparison-table__cell--feature comparison-table__cell--price-label" data-i18n="comparison.features.price">
          Prix
        </div>
        <div class="comparison-table__cell">
          <span class="comparison-table__price">$0</span>
        </div>
        <div class="comparison-table__cell">
          <span class="comparison-table__price">$9<span class="comparison-table__period">/mo</span></span>
        </div>
        <div class="comparison-table__cell comparison-table__cell--highlight">
          <span class="comparison-table__price comparison-table__price--highlight">0.-</span>
        </div>
      </div>
    </div>

    <p class="comparison__footer" data-animate data-i18n="comparison.footer">
      Le QR code brandé et les thèmes pro ? Chez Linktree, c'est $9/mois. Chez nous, c'est <strong>gratuit</strong>.
    </p>
  </div>
</section>

<style>
  .comparison-table__cell--header-inline {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .comparison-table__price-tag {
    display: inline-block;
    font-size: 0.6875rem;
    font-weight: 600;
    background: #fef3c7;
    color: #92400e;
    padding: 2px 6px;
    border-radius: 4px;
  }

  .comparison-table__partial {
    color: var(--color-text-muted);
    font-weight: 500;
  }

  .comparison-table__text {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
  }

  .comparison-table__cell--price-label {
    font-weight: 600;
    color: var(--color-text);
  }

  .comparison-table__period {
    font-size: var(--text-xs);
    font-weight: 400;
    color: var(--color-text-secondary);
  }

  .comparison-table__price--highlight {
    color: var(--color-primary-dark);
  }

  .comparison__footer {
    text-align: center;
    font-size: var(--text-body);
    color: var(--color-text);
    max-width: 500px;
    margin: 0 auto;
  }

  .comparison__footer strong {
    color: var(--color-primary-dark);
  }
</style>
