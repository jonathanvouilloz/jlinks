{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bun install",
  "buildCommand": "bun build src/vercel.ts --outfile api/index.js --target node --format esm --external @libsql/client --external drizzle-orm --external elysia --external @elysiajs/cors --external better-auth --external qrcode --external resend",
  "regions": ["fra1"],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
